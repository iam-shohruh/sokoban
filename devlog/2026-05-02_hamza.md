**Decisions:**

- deadlock detection is essential for pruning:

    - `static:` boxes in corners or against walls without goals

    - `dynamic:` boxes blocking each other in ways that prevent goal access

- instead of expanding every possible move, we'll validate states before queueing

- BFS grows exponentially; is_deadlock prevents exploring "doomed" branches

**Done:**

- Implemented is_deadlock function in SokobanEnv

- Integrated deadlock check into src/sokoban/search/bfs.py to prune invalid paths

- Verified memory usage reduction on early levels by tracking 'explored' set size