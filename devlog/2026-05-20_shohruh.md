## 2026-05-20 — @shohruh

Replaced SciPy-based assignment in A* with an internal Hungarian implementation for the box-goal heuristic.

**Done:**
- Removed `scipy.optimize.linear_sum_assignment` dependency from `src/sokoban/search/a_star.py`.
- Added `_hungarian_min_cost(cost_matrix)` to compute minimum-cost matching in-repo.
- Updated `hungarian_heuristic(...)` to use the internal solver directly.
- Ran a syntax sanity check with `python3 -m py_compile src/sokoban/search/a_star.py`.

**Decisions:**
- Kept the heuristic strategy (optimal box-goal assignment) unchanged, only swapped the implementation backend.
- Implemented a rectangular-matrix-compatible Hungarian variant (`rows <= cols`, transpose fallback) so it remains robust if matrix dimensions differ.
- Returned only total assignment cost (not mapping) since A* heuristic needs scalar cost only; this keeps the helper small and focused.

**Blocked / next:**
- No blockers in implementation.
- Next: run microban regression comparison for explored states and elapsed time to confirm no behavioral regression versus SciPy path.
