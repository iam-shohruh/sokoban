## 2026-04-30 - @Homr717


**Decisions:**

- Environment Integration: Chose to build the logic directly into env.py to bridge the gap between the repository structure and the actual Sokoban simulation.

    - Static/Dynamic Separation: Treated walls/goals as static and the player/boxes as dynamic to keep state tracking efficient.

    - Action Filtering: Decided to use a "scouting" approach in get_valid_actions to calculate legal moves before the agent attempts them, rather than correcting errors after a move is made.

    - Push Logic (Look-ahead): Implemented a check that looks "two steps ahead"—verifying both the box's position and the space behind it—to ensure a push is physically possible.

    - Using the deadlock in the bfs to reduce space complexity, and therefore reduce the time of the search.

**Done**:

- Handled the env.py file: Populated the environment file with the core movement and logic functions.

- Implemented the step function: Developed the primary logic for executing moves, which updates the player position and handles the shifting of boxes within the environment.

- Implemented get_valid_actions: Built the "scout" function that scans all four directions (UP, DOWN, LEFT, RIGHT) and returns a list of only the legal strings based on the current state.

- Added Collision Detection: Programmed logic within these functions to prevent the player and boxes from overlapping with static walls.

- Added deadlock check function to add to bfs.